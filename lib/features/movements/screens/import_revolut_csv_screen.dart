import 'package:flutter/material.dart';

class ImportRevolutCsvScreen extends StatelessWidget {
  const ImportRevolutCsvScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Importar CSV (pendiente)')),
      body: const Center(child: Text('Pantalla pendiente.')),
    );
  }
}
