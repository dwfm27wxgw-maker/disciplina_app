import 'package:flutter/material.dart';

class EditTickersScreen extends StatelessWidget {
  const EditTickersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Editar tickers (pendiente)')),
      body: const Center(child: Text('Pantalla pendiente.')),
    );
  }
}
